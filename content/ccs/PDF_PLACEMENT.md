# CCS PDF placement

Place the final course PDFs in these private, server-served folders:

```text
content/ccs/en/module-0.pdf through module-9.pdf
content/ccs/fr/module-0.pdf through module-9.pdf
```

Do not place course modules inside `public/`. Files in `public/` bypass candidate
authentication and can be opened by anyone who knows the URL.
